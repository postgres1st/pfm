# Postgres1st WatchTower Compatibility

This plugin provides a compatibility layer between Grafana and the Postgres1st WatchTower UI.

It servers as a messaging channel to sync state and communicate between an instance of Grafana embedded in an iframe and an outer Postgres1st WatchTower UI shell.
